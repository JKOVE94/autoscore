# Optional: absolute backend base URL. Leave empty to use the Vite dev proxy
# (/api -> http://127.0.0.1:8000, configured in vite.config.ts).
VITE_API_BASE=

# Backend target for the dev proxy (defaults to http://127.0.0.1:8000).
VITE_API_TARGET=http://127.0.0.1:8000
