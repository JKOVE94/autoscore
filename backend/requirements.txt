# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Music / notation ---
music21==9.3.0

# --- Audio analysis (Step 2) — fallback path (required) ---
# The whole backend targets Python 3.11 (no 3.14 wheels for the audio stack).
librosa==0.10.2.post1
numpy==1.26.4
soundfile==0.12.1

# --- Audio analysis — premium backends (optional) ---
# audio_analyzer imports these lazily and degrades to the librosa fallback when
# absent. Install when you want higher-quality transcription:
#   pip install basic-pitch==0.4.0     # melody (Spotify note transcription)
#   pip install madmom==0.16.1         # rhythm (RNN downbeat tracking)
#   pip install essentia               # key + chord (HPCP); Apple Silicon: build/altwheels

# --- Dev / test ---
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
ruff==0.9.2
